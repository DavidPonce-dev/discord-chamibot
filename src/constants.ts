// Re-export from config for backward compatibility
export { LOOP_LABELS } from "@/config/ui"
export { MAX_AUTOPLAY_SEC, MAX_RETRIES, GENRE_KEYWORDS, NON_MUSIC_KEYWORDS } from "@/config/radio"

// Queue-specific constant (not moved to config yet)
export const TRACKS_PER_PAGE = 3
